{
  "name": "myteluv2",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "concurrently -n BE,MO -c blue,green \"npm run backend\" \"npm run mobile:chrome\"",
    "start:macos": "concurrently -n BE,MO -c blue,green \"npm run backend\" \"npm run mobile:macos\"",
    "start:windows": "concurrently -n BE,MO -c blue,green \"npm run backend\" \"npm run mobile:windows\"",
    "start:ios": "concurrently -n BE,MO -c blue,green \"npm run backend\" \"npm run mobile:ios\"",
    "start:android": "concurrently -n BE,MO -c blue,green \"npm run backend\" \"npm run mobile:android\"",
    "start:all": "concurrently -n BE,MO -c blue,green \"npm run backend\" \"npm run mobile:all\"",
    "backend": "cd backend && nodemon server.js",
    "mobile": "cd mobile && flutter run",
    "mobile:chrome": "cd mobile && flutter run -d chrome",
    "mobile:macos": "cd mobile && flutter run -d macos",
    "mobile:windows": "cd mobile && flutter run -d windows",
    "mobile:ios": "cd mobile && flutter run -d ios",
    "mobile:android": "cd mobile && flutter run -d android",
    "mobile:all": "cd mobile && flutter run -d all",
    "install-all": "npm install && cd backend && npm install && cd ../mobile && flutter pub get",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
