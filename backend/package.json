{
  "name": "myteluv2_be",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma:reset": "npx dotenv -e ../.env -- prisma migrate reset --schema prisma/schema.prisma",
    "prisma:migrate": "npx dotenv -e ../.env -- prisma migrate dev --schema prisma/schema.prisma",
    "prisma:generate": "npx dotenv -e ../.env -- prisma generate --schema prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "node": "^22.21.0"
  },
  "devDependencies": {
    "dotenv-cli": "^10.0.0",
    "nodemon": "^3.1.10",
    "prisma": "^6.18.0"
  }
}
